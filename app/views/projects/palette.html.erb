<%= simple_form_for @project do |f| %>
  <%= f.select :palette_id, Palette.all.map { |r| [r.label, r.id] }, include_blank: false  %><br>
  <%= f.button :submit, "Save and continue" %>
<% end %>

<%= link_to 'Create Palette', new_palette_path %>